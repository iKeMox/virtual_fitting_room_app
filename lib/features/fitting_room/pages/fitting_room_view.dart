import 'package:flutter/material.dart';

class FittingRoomView extends StatelessWidget {
  const FittingRoomView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Fitting Room", style: TextStyle(color: Colors.black),)
    );
  }
}
